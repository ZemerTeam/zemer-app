<?xml version="1.0" encoding="utf-8"?>
<!-- The launcher icon's real background: solid black, matching the artwork baked into the
     foreground webp. (This replaced the leftover Android Studio template green grid, which was
     never visible only because the opaque foreground covered it - issue #179.) -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FF000000"
        android:pathData="M0,0h108v108h-108z" />
</vector>
